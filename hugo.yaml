params:
  alumni:
    menu:
      programs:
        exclude_children: false
  persons:
    index:
      options:
        summary: false
    single:
      backlinks: true
  programs:
    share_links:
      enabled: false